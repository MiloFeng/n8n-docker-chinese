# 🇨🇳 n8n 中文界面配置

## ⚡ 一键配置

```bash
./scripts/setup-chinese.sh
```

---

## 🔧 手动配置

### 1. 编辑 docker-compose.yml

在 `n8n` 服务的 `environment` 部分添加:

```yaml
- N8N_DEFAULT_LOCALE=zh-CN
```

在 `volumes` 部分添加:

```yaml
- ./n8n-chinese-ui/dist:/usr/local/lib/node_modules/n8n/node_modules/n8n-editor-ui/dist
```

### 2. 重启服务

```bash
docker compose down
docker compose up -d
```

---

## 🔍 问题诊断

### 重启后变回英文?

```bash
# 运行诊断
./scripts/diagnose-chinese.sh

# 检查配置
docker compose exec n8n env | grep LOCALE
```

**常见原因**:
1. ❌ 环境变量加错位置 (加到了 postgres 而不是 n8n)
2. ❌ 缺少 UI 文件挂载
3. ❌ 中文 UI 文件未下载

**解决方案**:
```bash
# 重新配置
./scripts/setup-chinese.sh
```

---

## 📚 原理说明

中文汉化需要两部分:

1. **环境变量** `N8N_DEFAULT_LOCALE=zh-CN`
   - 告诉 n8n 使用中文

2. **中文 UI 文件** (关键!)
   - 替换默认的英文界面文件
   - 来源: https://github.com/other-blowsnow/n8n-i18n-chinese

---

## 🔗 相关资源

- 中文汉化项目: https://github.com/other-blowsnow/n8n-i18n-chinese
- 中文 Docker 镜像: https://hub.docker.com/r/hotwa/n8n-chinese

